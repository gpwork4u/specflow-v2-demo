{
  "name": "hr-system-e2e-tests",
  "version": "1.0.0",
  "description": "E2E + Browser tests for HR system (Sprint 1-4)",
  "private": true,
  "scripts": {
    "test:api": "npx playwright test --config=playwright.config.ts --project=api",
    "test:browser": "npx playwright test --config=playwright.config.ts --project=browser",
    "test:all": "npx playwright test --config=playwright.config.ts",
    "test:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
